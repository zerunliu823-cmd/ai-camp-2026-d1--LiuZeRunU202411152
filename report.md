# 每日作业报告

> 复制为本日独立仓库根目录的 `report.md`。删除本提示，全部换成你们自己的真实证据。不要让智能体替学生写反思或结论。

## 1. 本日问题

- 里程碑：day-01
- 学生或小组：刘泽润 韦佳瑜 吴升峰 程霖垚
- 使用者：准备历史展览、需要解释模型局限的博物馆教育团队。
- 真实输入：指定 Kaggle 页面下载的 Titanic train.csv，891 行、12 列真实历史乘客记录。
- 需要的输出：多数类基线和随机森林候选在同一留出测试集上的指标、混淆矩阵与真实误判行。
- 与使用者最相关的错误：假阴性——真实幸存者被预测为未幸存；也要如实记录假阳性
- 本日产品边界：这是历史观察数据分析，不是现代救援分配工具；不能用关联证明某个字段导致幸存或死亡。

## 2. 真实数据或真实课程输入

- 所有者/发布者：赛义德·哈姆扎·阿里
- 标题：泰坦尼克号数据集
- 原始 URL：https://www.kaggle.com/datasets/hesh97/titanicdataset-traincsv
- 许可标签或使用许可：CC0：公有领域
- 下载/取得日期：20260817
- 预期文件与结构：train.csv，页面显示约 61.19 kB、12 列
- 检查命令：python train.py --check-data
- 实际检查结果：REAL DATA CHECK PASSED、891 行、12 列。
- 已知缺失、偏差或限制：Age 有 177 个缺失值、Embarked 有 2 个缺失值；流水线会按训练数据规则填补，填补后的值不等于真实未知信息。
  数据只包含 891 条历史乘客记录和有限字段；它没有记录所有可能影响当时结果的情境信息。
  这是历史观察数据，只能评价这份数据上的预测表现，不能证明舱位、性别、年龄等字段导致幸存或死亡，也不适用于现代救援分配。

Day 0 请把“真实输入”写成实际课程仓库、Day 1 文件和 `learning-plan.json`，并记录路径检查。

## 3. 可复现运行

```powershell
# 当前目录
Get-Location
E:\Users\ROG\Downloads\ai-summer-camp-2026-main\student-work\day-01-titanic
# 安装
python -m pip install -r requirements.txt

# 数据检查
python train.py --check-data

# 主程序
python train.py

# 测试
python -m unittest discover -s tests -v

数据检查通过、测试为 OK，主程序输出 metrics.json 与 errors.csv。

写出关键预期输出与实际输出文件位置。不要只写“运行成功”。

## 4. 基线与候选

### 简单基线

- 方法：多数类基线，始终预测训练集最常见的类别。
- 为什么足够简单：不学习乘客特征，只输出同一类别。
- 命令：python train.py
- 结果：准确率约 0.614；幸存者召回率 0；F1 0

### 候选方法

- 学生完成的核心改动：在原有预处理后加入固定随机种子 42 的随机森林。
- 保持不变的数据、划分、指标或参数：真实数据、特征、固定训练/测试划分、预处理和评估指标
- 命令：python train.py
- 结果：准确率约 0.744；幸存者召回率约 0.628；F1 约 0.655。

| 项目 | 基线 | 候选 |含义
| --- | ---: | ---: |比较必须是在同一留出测试集上，候选仍会漏掉真实幸存者。
| F1（幸存者） | 0 | 0.655 |
| 假阴性数量 | 86 | 32 |

## 5. 一个真实失败案例

- 样本位置/编号：source_row 347，PassengerId 348
- 真实结果：1（幸存）
- 系统输出：0（预测未幸存）
- 可以观察到什么：三等舱、女性、年龄缺失、SibSp=1、Fare=16.1、Embarked=S
- 说明的限制：模型仍会漏掉真实幸存者。
- 不能证明什么：不能证明任一字段导致幸存或导致误判。
- 下一项最小检查：查看更多假阴性，确认它们是否呈现相似模式，而不凭一条记录下结论。

## 6. 智能体与学生工作边界

- 智能体提出/生成/修改了什么：解释术语、给出小步骤、说明数据路径和检查命令、根据我们贴出的输出解释结果。
- 学生怎样核对文件、来源、输出、测试和 diff：登录并下载真实数据、复制文件、运行命令、在工作副本中修改并保存 train.py、运行测试、选择并核对真实错误行。
- 学生修改或拒绝了什么建议：学生在核对基线、测试和课程边界后，亲自修改了 train.py：导入随机森林，并把候选模型放在既有预处理之后且固定随机种子。学生没有修改数据、测试、数据划分或指标；本次没有拒绝建议的情况。
- 每名成员能独立解释的代码或证据：多数类基线、候选流水线、假阴性、以及“相关不等于因果”

## 7. 结论与限制

我们使用Titanic Dataset train.csv，在223条留出记录上比较 多数类基线与 随机森林。候选模型的幸存者 F1 从0提升到0.655。候选模型仍有32个假阴性，表示真实幸存者被预测为未幸存。source_row 347 是一条真实幸存但被候选模型预测为未幸存的记录，说明模型仍会漏掉部分幸存者。数据中 Age 有 177 个缺失值、Embarked 有 2 个缺失值，且可用字段有限。本实验只在这份固定留出测试集上评估，因此结果不代表所有历史记录或其他场景。这些结果只描述历史数据中的预测关联，不能证明舱位、性别、年龄或其他字段导致幸存或死亡。明确该程序不能用于现代救援分配或其他高风险现实决策。

## 8. 提交复核

- [ ] README 从新环境可以开始运行
- [ ] 数据检查、测试和主程序重新运行
- [ ] 报告数字与保存输出一致
- [ ] `presentation.pptx` 在 3 分钟内讲完
- [ ] `submission.json` 路径正确
- [ ] 无密钥、大数据、私人信息、虚拟环境或缓存
- [ ] GitHub 网页复查并邮件发送 URL
